apiVersion: v1
kind: Pod
metadata:
    name: my-first-pod
    labels:
        app: webserver
spec:
    containers:
        - name: pod-demo
          image: chapin/docker-demo
          ports:
              - containerPort: 3000

